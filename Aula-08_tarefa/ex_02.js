// Defina uma Arrow Function chamada calculaAreaRetangulo que recebe dois parâmetros, base e altura, e retorna a área do retângulo. Mostre o resultado no console.

const calculaAreaRetangulo = (base, altura) => {
  return base * altura;
}

console.log(calculaAreaRetangulo(2, 10))
